{
	"name": "@orion/api",
	"private": true,
	"scripts": {
		"dev": "SCAN_DIR=${SCAN_DIR:-data/dev-scans} uv run --directory ../.. uvicorn services.api.api:app --host 0.0.0.0 --port 5171 --reload --reload-dir src/services"
	}
}

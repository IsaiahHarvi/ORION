apiVersion: v2
name: orion
description: Observation, Reconnaissance, Intelligence, and Operations Network
type: application
home: https://orion.harville.dev
sources:
  - https://github.com/isaiah-harville/ORION
# version is the chart's own revision; appVersion tracks the release tag and is
# the default tag for every ORION image. Release tooling rewrites appVersion
# from the git tag, so a chart install pins one coherent set of images.
version: 0.2.1
appVersion: "0.2.1"

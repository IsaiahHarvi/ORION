services:
  fast:
    build:
      context: ../../
      dockerfile: deploy/fast/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - SCAN_DIR=/app/scans
      - KERAS_BACKEND=torch
      - TORNET_ROOT=data/TorNet
      - VITE_API_URL=${API_MODE:-production}  # if not set, defaults to production

    # volumes:
      # - ../../data/TorNet:/app/data/TorNet

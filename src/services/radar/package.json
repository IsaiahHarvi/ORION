{
	"name": "@orion/radar",
	"private": true,
	"scripts": {
		"dev": "SCAN_DIR=${SCAN_DIR:-data/dev-scans} UV_CACHE_DIR=.cache/uv PYTHONPATH=src uv run --directory ../../.. --python 3.11 --no-project --with-requirements deploy/api/requirements.in python -m services.radar.producer"
	}
}
